# Feed Sales AI MVP v1.6.1 - P0 问题修复计划

**创建日期**: 2026-03-28  
**优先级**: P0 (Critical)  
**目标**: 100% 架构合规，准备 v1.6.2 部署  

---

## 执行摘要

### 当前状态：75% 合规 (条件通过)

v1.6.1 代码已提交，核心架构基本符合 v1.6 设计要求，但存在**3 项 P0 违规**必须在 v1.6.2 中修复。

---

## 1. P0 问题清单

### P0-01: ❌ `agent/` 目录未删除

**违规描述**: v1.6 架构要求删除 `agent/` 目录，由 OpenClaw Triggers 替代

**当前状态**: 
- `agent/` 目录仍存在于 master 分支根目录
- 违反 v1.6 架构 Req #1

**影响**: 
- 代码冗余，增加维护成本
- 与 OpenClaw 触发器模式冲突

**修复方案**:
```bash
# 1. 验证 agent/ 目录内容
ls -la agent/

# 2. 删除 agent/ 目录
rm -rf agent/

# 3. 提交变更
git add -A
git commit -m "P0-fix: remove agent/ directory per v1.6 architecture (replaced by OpenClaw Triggers)"
```

**验收标准**:
- [ ] `agent/` 目录不存在于根目录
- [ ] Git 提交记录包含删除操作
- [ ] 无代码引用 `agent/` 目录

**预计工时**: 0.5h

---

### P0-02: ❌ `llm_extractor.py` 未删除

**违规描述**: v1.6 架构要求删除 `llm_extractor.py`，使用 OpenClaw 内置 LLM

**当前状态**: 
- `llm_extractor.py` 仍存在于根目录
- 违反 v1.6 架构 Req #2

**影响**: 
- 重复实现 LLM 调用逻辑
- 无法利用 OpenClaw 平台能力

**修复方案**:
```bash
# 1. 验证 llm_extractor.py 使用情况
grep -r "llm_extractor" --include="*.py" .

# 2. 删除 llm_extractor.py
rm llm_extractor.py

# 3. 提交变更
git add -A
git commit -m "P0-fix: remove llm_extractor.py (using OpenClaw built-in LLM)"
```

**验收标准**:
- [ ] `llm_extractor.py` 文件不存在
- [ ] 无代码引用 `llm_extractor` 模块
- [ ] Git 提交记录包含删除操作

**预计工时**: 0.5h

---

### P0-03: ❌ `skill.py` 仍使用 `llm_extractor` 导入

**违规描述**: `skill.py` 仍在使用 `from .llm_extractor import LLMExtractor`

**当前状态**: 
- `skills/formula_cost_skill/skill.py` 第 1 行导入 `llm_extractor`
- 违反 v1.6 架构 Req #3

**影响**: 
- 代码无法运行（导入失败）
- 需要重构为使用 `context.intent`

**修复方案**:

**当前代码** (需要删除):
```python
# skills/formula_cost_skill/skill.py
from .llm_extractor import LLMExtractor  # ❌ 删除此行
```

**修复后代码**:
```python
# skills/formula_cost_skill/skill.py
from typing import Dict, Any, Optional
from skills.base_skill import BaseSkill

class FormulaCostSkill(BaseSkill):
    """配方成本计算 Skill"""
    
    @classmethod
    def execute(cls, context: Dict[str, Any]) -> Dict[str, Any]:
        """
        执行配方成本计算
        
        Args:
            context: OpenClaw 上下文，包含 intent、entities 等
            
        Returns:
            计算结果字典
        """
        # ✅ 使用 context.intent 而非 LLMExtractor
        intent = context.get('intent', 'formula_cost_calculation')
        entities = context.get('entities', {})
        
        # 提取配方信息
        formula_name = entities.get('formula_name')
        custom_ingredients = entities.get('ingredients')
        
        # 调用核心计算逻辑
        result = cls.calculate_formula_cost(formula_name, custom_ingredients)
        
        return {
            'success': True,
            'data': result,
            'intent': intent
        }
```

**验收标准**:
- [ ] `skill.py` 不再导入 `llm_extractor`
- [ ] 使用 `context.intent` 处理意图
- [ ] 代码通过单元测试

**预计工时**: 1h

---

## 2. 修复时间表

### Day 1: P0 问题修复 + 单元测试 (2026-03-28)

| 时间 | 任务 | 负责人 | 状态 |
|------|------|--------|------|
| 09:00-09:30 | P0-01: 删除 `agent/` 目录 | Kenny | ⏳ 待开始 |
| 09:30-10:00 | P0-02: 删除 `llm_extractor.py` | Kenny | ⏳ 待开始 |
| 10:00-11:00 | P0-03: 重构 `skill.py` 使用 `context.intent` | Kenny | ⏳ 待开始 |
| 11:00-12:00 | 编写 P0 修复单元测试 | Kenny | ⏳ 待开始 |
| 14:00-15:00 | 执行单元测试，验证修复 | Kenny | ⏳ 待开始 |
| 15:00-16:00 | 提交 v1.6.2-alpha 到 Gitee | Kenny | ⏳ 待开始 |

---

## 3. 测试验证计划

### 3.1 单元测试 (P0 修复后)

**测试文件**: `tests/test_p0_fix.py`

```python
import pytest
import os
from pathlib import Path

def test_agent_directory_removed():
    """验证 agent/ 目录已删除"""
    agent_path = Path('agent')
    assert not agent_path.exists(), "agent/ 目录应该被删除"

def test_llm_extractor_removed():
    """验证 llm_extractor.py 已删除"""
    llm_extractor_path = Path('llm_extractor.py')
    assert not llm_extractor_path.exists(), "llm_extractor.py 应该被删除"

def test_skill_no_llm_extractor_import():
    """验证 skill.py 不再导入 llm_extractor"""
    skill_path = Path('skills/formula_cost_skill/skill.py')
    with open(skill_path, 'r', encoding='utf-8') as f:
        content = f.read()
    assert 'llm_extractor' not in content, "skill.py 不应导入 llm_extractor"

def test_skill_uses_context_intent():
    """验证 skill.py 使用 context.intent"""
    skill_path = Path('skills/formula_cost_skill/skill.py')
    with open(skill_path, 'r', encoding='utf-8') as f:
        content = f.read()
    assert 'context.intent' in content or "context.get('intent'" in content, \
        "skill.py 应使用 context.intent"
```

### 3.2 集成测试

**测试文件**: `tests/test_integration_v1.6.py`

```python
import pytest
from skills.formula_cost_skill.skill import FormulaCostSkill

def test_formula_cost_skill_with_context():
    """测试配方成本 Skill 使用 context 模式"""
    context = {
        'intent': 'formula_cost_calculation',
        'entities': {
            'formula_name': '标准配方 1',
            'owner_open_id': 'test_user'
        }
    }
    
    result = FormulaCostSkill.execute(context)
    
    assert result['success'] == True
    assert 'data' in result
    assert result['intent'] == 'formula_cost_calculation'
```

---

## 4. 验收标准

### 4.1 架构合规性 (100%)

- [ ] ✅ `agent/` 目录已删除
- [ ] ✅ `llm_extractor.py` 已删除
- [ ] ✅ `skill.py` 使用 `context.intent`
- [ ] ✅ SQLite WAL + `owner_open_id` 隔离
- [ ] ✅ `skill.yaml` triggers 配置完整

### 4.2 测试覆盖率 (100%)

- [ ] ✅ P0 修复单元测试全部通过
- [ ] ✅ 集成测试全部通过
- [ ] ✅ 无回归测试失败

### 4.3 代码质量 (85+ 分)

- [ ] ✅ 无 P0/P1 级别代码异味
- [ ] ✅ 所有函数有文档字符串
- [ ] ✅ 遵循 PEP 8 规范

---

## 5. Git 提交计划

### 提交 1: 删除 `agent/` 目录
```bash
git add -A
git commit -m "P0-fix: remove agent/ directory per v1.6 architecture

- Delete agent/ directory (replaced by OpenClaw Triggers)
- Update .gitignore to prevent future agent/ creation
- Related: v1.6 architecture compliance

Issue: v1.6.1-audit-001"
```

### 提交 2: 删除 `llm_extractor.py`
```bash
git add -A
git commit -m "P0-fix: remove llm_extractor.py (using OpenClaw built-in LLM)

- Delete llm_extractor.py
- Remove all references to LLMExtractor
- Migrate to OpenClaw built-in LLM

Issue: v1.6.1-audit-002"
```

### 提交 3: 重构 `skill.py`
```bash
git add -A
git commit -m "P0-fix: refactor skill.py to use context.intent

- Remove from .llm_extractor import LLMExtractor
- Use context.intent for intent handling
- Update execute() signature to match OpenClaw standard
- Add comprehensive docstrings

Issue: v1.6.1-audit-003"
```

### 提交 4: 添加 P0 修复测试
```bash
git add -A
git commit -m "test: add P0 fix verification tests

- Add test_p0_fix.py with 4 test cases
- Add integration tests for context.intent mode
- All tests passing

Issue: v1.6.1-audit-004"
```

### 提交 5: 更新文档
```bash
git add -A
git commit -m "docs: update README for v1.6.2

- Update architecture description
- Add OpenClaw Skill mode explanation
- Remove references to agent/ and llm_extractor.py

Issue: v1.6.1-audit-005"
```

---

## 6. 部署验证

### 6.1 本地验证

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 执行 P0 修复测试
cd tests/
python -m pytest test_p0_fix.py -v

# 3. 执行集成测试
python -m pytest test_integration_v1.6.py -v

# 4. 验证架构合规性
python scripts/verify_architecture.py
```

### 6.2 Gitee 部署

```bash
# 1. 推送到 Gitee master 分支
git push origin master

# 2. 创建 v1.6.2 标签
git tag v1.6.2
git push origin v1.6.2

# 3. 创建 Release
# https://gitee.com/kenny-chenym/feed-sales-ai-mvp/releases/new
```

---

## 7. 回滚计划

如果 v1.6.2 部署失败，执行以下回滚操作：

```bash
# 1. 回滚到 v1.6.1 标签
git checkout v1.6.1

# 2. 强制推送回滚
git push origin master --force

# 3. 通知团队
# 使用飞书技能发送通知
```

---

## 8. 成功标准

### v1.6.2 部署成功标志：

1. ✅ **架构合规性 100%**: 所有 P0 问题已修复
2. ✅ **测试通过率 100%**: 所有单元测试、集成测试通过
3. ✅ **代码质量 85+ 分**: 无 P0/P1 代码异味
4. ✅ **文档完整性**: README、ARCHITECTURE 已更新
5. ✅ **单用户 MVP 可部署**: 可部署给单一用户测试

---

## 9. 下一步

### v1.6.2 完成后：

1. **P1 任务**: Barchart API 真实价格源接入
2. **P2 任务**: 用户文档完善
3. **v1.7 规划**: 多用户测试 + 性能优化

---

**批准**: Kenny Chen  
**审核日期**: 2026-03-28  
**下次更新**: 2026-03-28 (Day 1 结束)
